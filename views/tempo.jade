html
    head

    body
        h1 Tmp page
            - for (var i = 0; i < arr.length; i++)
                div #{typeof(arr[i])}
                - each val, key in ['v0', 'opp1', 'v1', 'opp2', 'v1']
                    if arr[i][val] == '?'
                        input(type='text')
                    else
                        span #{arr[i][val]}
